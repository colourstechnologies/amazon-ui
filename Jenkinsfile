
@Library('jenkins-ci-cd-library') _

pipeline {
    agent any
    stages {
        stage('Addition Stage') {
            steps {
                add(15, 25) // Hardcoded values
            }
        }
    }
}
